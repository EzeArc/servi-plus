<!-- ---
import ProviderCard from "./ProviderCard.astro";
import apiRoutes from "@/api";

const data = await fetch(`${apiRoutes.users}/listProvidersActive`).then(
  (response) => response.json()
);
const listProvidersActive = data;
console.log(listProvidersActive);
---

<section
  class="max-w-6xl mx-auto flex flex-col gap-4 pt-20 justify-center items-center px-20"
>
  <h2
    class="text-4xl lg:text-6xl text-center mb-10 lg:mb-20 uppercase font-tomaso"
  >
    Bienvenido admin
  </h2>
  <div class="flex flex-col lg:flex-row gap-10">
    <div class="flex flex-col gap-2">
      {
        listProvidersActive.map((item) => {
          item.forEach((provider) => {
            <div class="flex gap-3 items-end mr-5">
              <ProviderCard />
            </div>;
          });
        })
      }
    </div>
  </div>
</section> -->
